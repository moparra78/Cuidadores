<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cuidadores">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Habitacion"></activity>
        <activity android:name=".Listaespacios" />
        <activity android:name=".Pacientelista" />
        <activity android:name=".Segundalista" />
        <activity android:name=".Plan" />
        <activity android:name=".Medicacion" />
        <activity android:name=".Fase" />
        <activity android:name=".Antiguedad" />
        <activity android:name=".Primeralista" />
        <activity
            android:name=".Bienvenida"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>